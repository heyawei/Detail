let str = 'JavaScript is the best lang';
          '0123456789'; // 下标
          '123456789'; // 个数

//let hasIs = str.indexOf('iss') === -1 ? false : true;

//let has = str.includes('Java', 4);

//console.log(has);

let has = str.startsWith('Script', 0);
console.log(has);

let has2 = str.endsWith('Java', 4);
console.log(has2);

let strok = 'okay';

console.log(strok.repeat(2));